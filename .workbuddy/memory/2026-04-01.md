# 2026-04-01 日志

## 小红书工作流部署问题

**问题**：将扣子工作流API部署到个人网站时，Vercel代理返回500错误

**已尝试**：
1. 前端直接调用扣子API → CORS错误
2. Vercel Serverless Function代理 → 500错误
3. 简化版代理 → 未测试（网络问题）

**关键发现**：
- Vercel Serverless Function中 `req.body` 不会自动解析JSON，必须用 `for await (const chunk of req)` 手动读取
- 扣子API支持base64格式图片，不需要图床
- GitHub推送可能受网络限制

**已整理文档**：
1. 完整问题记录（包含所有密钥和代码）
2. 代码文件包（可直接使用的代码）
3. API调试指南
4. 一页纸问题总结

**用户反馈**：部署失败，准备发给其他AI尝试

**反思**：应更系统性地测试和简化解决方案，考虑网络环境限制